package basics_of_OOP;

import recursive.BinarySearch;

import java.util.Arrays;
import java.util.LinkedList;

public class Solution {
    public static void main(String[] args) {
/*        Vector vector = new Vector(2,2,2);
        Vector otherVector = new Vector(3,4,5);

        System.out.println("Length of vector = " + vector.lengthOfVector());
        System.out.println("Length of otherVector = " + otherVector.lengthOfVector());

        System.out.println("Scalar of vectors = " + vector.scalarOfVectors(otherVector));
        System.out.println("Vector mul of vectors = " + Arrays.toString(vector.vectorOfVectors(otherVector)));
        System.out.println("Corner of vectors = " + vector.cornerOfVectors(otherVector));

        System.out.println("Add of vectors = " + vector.add(otherVector));
        System.out.println("Sub of vectors = " + vector.sub(otherVector));

        int num = 5;
        System.out.println("Array of " + num + " vectors: ");
        for (int i = 0; i < num; i++){
            System.out.println(Vector.arrayOfVectors(num)[i]);
        }*/

/*        int[] el = {1,2,3};
        int[] weight = {1,2,4};
        WeightArray weightArray = new WeightArray(3, el, weight);

        int num = 7;
        for (int i = 0; i < num; i++){
            System.out.println("This is " + i + " number " + weightArray.weightReturn());
        }*/

    }
}
